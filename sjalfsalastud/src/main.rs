use std::io;

fn main() {
    let mut input = String::new();
    io::stdin().read_line(&mut input).unwrap();
    
    let inntak_vector: Vec<i32> = input.split_whitespace().map(|i| i.parse::<i32>().unwrap()).collect();

    let total_cans = inntak_vector[0]
    let tikallar = inntak_vector[1]
    let fimmtiu = inntak_vector[2]
    let hundrad = inntak_vector[3]

}

fn afgangur(penge_type) {
    if penge_type == 100 {
        
    }
}