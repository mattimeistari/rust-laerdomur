use std::io;

fn main() {
    let mut input = String::new();
    io::stdin().read_line(&mut input).unwrap();
    
    input.clear();
    io::stdin().readline(&mut input).unwrap();
    
    let p: f32 = input.trim().parse().unwrap();
    
    input.clear();
    let n: i32 = input.trim().parse().unwrap();
    
    let mut plast_fjoldi = 0
    for _ in 0..n {
        
    }
    //I <3 you, Eldur!
    
    
}
